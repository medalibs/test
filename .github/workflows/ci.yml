name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - name: Run a multi-line script
      run: |
        echo ----------------------
        php index.php
        echo ----------------------
