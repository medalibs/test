name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - name: Run a multi-line script
      run: |
        echo ----------------------
        apt-get update -y
        apt-get install -y php-cli
        php index.php
        echo ----------------------
